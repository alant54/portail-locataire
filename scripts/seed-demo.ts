/**
 * STUB — lane B replaces the body, keeping the file path and the exit code.
 *
 * Wired into `npm run setup` by phase-0-hardening so that lane B never has to edit
 * the frozen `package.json`. Until lane B lands it is a deliberate no-op: `setup`
 * must keep working from a bare clone.
 *
 * Lane B fills this with: 2 tenant accounts (TEN-00005, TEN-00170) + 1 manager,
 * password-hashed, credentials documented in the README.
 */
console.log("  seed:demo — stub, no demo accounts yet (lane B)");
